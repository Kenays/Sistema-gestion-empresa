services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: empresa-sql
    env_file:
      - .env
    environment:
      - ACCEPT_EULA=Y
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data:

